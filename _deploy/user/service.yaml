apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: user-api
spec:
  hosts:
    - "guam-api.wafflestudio.com"
  gateways:
    - istio-ingress/waffle-ingressgateway
  http:
    - route:
        - destination:
            host: user-api
