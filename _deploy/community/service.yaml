apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: community-api
spec:
  hosts:
    - "guam-community-api.wafflestudio.com"
  gateways:
    - istio-ingress/waffle-ingressgateway
  http:
    - route:
        - destination:
            host: community-api
