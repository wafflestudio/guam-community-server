apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: favorite-api
spec:
  hosts:
    - "guam-favorite.wafflestudio.com"
  gateways:
    - istio-system/waffle-ingressgateway
  http:
    - route:
        - destination:
            host: favorite-api
