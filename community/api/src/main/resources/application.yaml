spring:
  config:
    import:
     - application-db.yaml
     - application-r2dbc.yaml
     - application-service.yaml
---
spring:
  jpa:
    defer-datasource-initialization: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  config:
    activate:
      on-profile: dev
